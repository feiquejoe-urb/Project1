# Common Ground 开发进度

最后更新：2026-08-23  
产品目录：`D:\CC\CommonGround`

## 当前阶段

共享 Demo 后端代码与数据库迁移已经完成，正式构建通过。产品支持在本地 IndexedDB 模式与 Supabase 云端模式之间按环境变量切换；待在 Supabase 执行迁移并配置 Vercel 环境变量后，即可启用跨设备共享。

## 云端 Demo 更新（2026-08-23）

- 新增 Supabase 匿名身份，不要求学生注册账号。
- 新增 Postgres 表、外键、约束和 Row Level Security；所有人可读，只有上传者可改删。
- 新增三个私有 Storage bucket，分别保存空间数据、观察图片和 PDF。
- 空间数据超过 6 MB 时使用 TUS 断点续传，满足最大 50 MB Shapefile ZIP。
- 原始文件按需下载，标准化 GeoJSON 用于地图渲染。
- 图层数据替换先上传和验证新数据，再原子切换引用并清理旧文件。
- 图片/PDF 使用有时效的签名 URL 预览，不公开暴露 bucket。
- 图层、观察、附件和评论变更通过 Realtime 通知其他在线浏览器刷新。
- 本地筛选、图层可见性和临时透明度不会被全班实时同步。
- 保留 IndexedDB 本地模式，缺少云端环境变量时不会影响现有 Demo。

## 第二轮更新（2026-08-23）

- 固定录入六个 Planning System；上传图层和新增观察时必须选择 System。
- 地图可按一个或多个 System 同时筛选图层与观察点。
- 图层可见性、临时透明度和导出选择已从共享图层数据移出。
- IndexedDB 升级至 v2，图层元数据与大型数据集分开保存。
- 图层替换采用先校验、后切换、再清理旧数据集的事务流程。
- 字段缺失或类型改变时，样式安全回退为单色，避免失效字段映射。
- 田野观察底层改用标准 GeoJSON Point。
- 每个观察支持最多 5 张压缩图片，或单个不超过 10 MB 的 PDF，两种模式不可同时选择。
- PDF 可在观察详情中预览或下载。
- 观察点支持无嵌套的简单班级评论。
- 上传者可永久删除自己的图层、观察点及其关联本地文件。
- 手机端支持相机图片输入、当前位置采集入口和触控友好的响应式布局。
- GeoJSON 限制 20 MB，Shapefile ZIP 限制 50 MB，单图层限制 30,000 个要素。
- 拒绝明显不处于 WGS84 经纬度范围的数据。
- v1 本地记录可自动迁移至 v2，旧内容默认归入 System 1。
- 新增 `DATA_MODEL_V2.md` 记录正式数据边界和未来服务器迁移方式。

## 已完成功能

### 地理数据

- 上传 GeoJSON、JSON 与 ZIP 格式 Shapefile。
- 自动读取点、线、面几何并缩放至数据范围。
- 多图层开关、透明度调整和贡献者信息展示。
- 图层用途、信源与处理方式元数据。
- 选择多个图层并打包下载 ZIP，同时附带标准化 GeoJSON 和元数据。
- 同一设备可编辑或下架自己的图层，不提供永久删除和历史版本。

### 快速图层可视化

- 上传时或上传后选择用于可视化的属性字段。
- 支持同色、唯一值、渐变三种方式。
- 内置 6 套配色模板。
- 地图详情显示对应图例。
- 唯一值最多处理 30 个类别；渐变仅接受数字字段。

### 属性查看

- 点击地图要素显示全部非空属性。
- 图层与地图要素详情中提供属性摘要。
- 摘要包括行数、字段数、数字或文字类型、缺失值、唯一值，以及数字字段的最小值、中位数、平均值和最大值。
- 只读完整属性表，每页 50 行，可翻页和横向滚动。

### 地图点位

- 新建点位可附带标题、备注、压缩图片和 OneDrive 链接。
- 同一设备可编辑或下架自己的点位。
- 按贡献者形成可开关的点位图层和预览列表。
- 新建或编辑点位必须选择城市观察类型：
  - Activity：活动、日常行为、聚集与移动。
  - Place-making：空间占用、改造、维护与地方营造。
  - Spatial condition：形态、边界、通达性、设施与土地使用。
  - Story & memory：个人故事、口述历史与集体记忆。
  - Documentation：照片证据、档案材料与田野记录。
  - Issue & opportunity：冲突、风险、缺失与改造潜力。
- 六类点位使用固定语义颜色，侧边栏与详情均显示类型。
- 点位尺寸已较初版缩小 20%。
- 点位图层始终位于空间数据图层上方；重叠点击时优先打开点位详情。
- 旧版无类型点位向后兼容为 Documentation。

### 本地运行与存储

- 项目保存在 D 盘，不依赖 C 盘下载目录。
- 可双击 `启动本地小样.cmd` 启动，也可运行 `npm run dev`。
- 使用 IndexedDB 保存图层、原始文件、GeoJSON、图片和点位。
- 使用浏览器 localStorage 保存贡献者名称与设备 ID。
- 图片使用对象 URL 按组件生命周期创建和释放，已修复反复查看后失效的问题。

## 已验证状态

- 生产构建 `npm run build` 通过。
- 曾使用 6,377 个要素的 Land_Use 图层验证属性摘要、唯一值与渐变可视化。
- Shapefile ZIP 与 GeoJSON 导入流程可用。
- 属性表分页可用。
- 点位图片反复打开可正常显示。
- 点位与面图层重叠时，点击优先进入 FIELD PIN 详情。
- 本轮结束时已清除本地网页测试数据和贡献者身份，源码及构建产物未删除。

## 当前限制

- 所有数据仅保存在当前浏览器，其他同学或设备无法同步看到。
- 尚无真正的登录、权限或班级成员管理。
- 尚无服务器文件存储、数据库、备份或容量配额。
- 图层下架仅改变状态，不提供恢复界面。
- 地图可视化以快速表达为目标，不支持手动修改单个颜色、线宽、分级断点或复杂符号。
- 大型 GeoJSON 仍在浏览器主线程中解析，数据继续增大后可能需要 Web Worker 或服务端预处理。
- 当前生产包体积较大，但不影响本地小样使用。
- 当前身份仍绑定单个浏览器，更换设备或清除浏览器数据后无法继续编辑原贡献。
- 当前评论也是本地数据，只有接入共享后端后才能跨设备协作。
- System 列表目前固定在代码中，没有教师管理界面。

## 下一轮建议优先级

1. 将 repository 接口接入轻量关系数据库和对象存储，支持约 30 人跨设备共享。
2. 为匿名浏览、昵称贡献增加服务器端编辑密钥校验。
3. 使用真实课程样本重新校准文件大小和要素数量限制。
4. 根据真实手机测试完善定位失败、弱网和表单草稿恢复。
5. 后续再考虑搜索、排序和服务端大型文件处理，不优先扩展复杂制图或空间分析。

## 数据清理说明

本轮结束时仅清除了 `http://127.0.0.1:5173` 对应应用的 IndexedDB 与 localStorage，包括测试图层、点位、图片、贡献者名称和设备 ID。项目源码、依赖、启动脚本、README、构建产物及本文件均保留。
